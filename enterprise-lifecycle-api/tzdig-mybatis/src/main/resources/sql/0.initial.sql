# create schema enterprise_lifecycle collate utf8mb4_general_ci;
# create user enterprise_lifecycle identified by 'enterprise_lifecycle';
# grant all privileges on enterprise_lifecycle.* to enterprise_lifecycle;

set names utf8mb4;
use enterprise_lifecycle;
